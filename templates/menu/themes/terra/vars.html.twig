<style>
:root {
    /* ── Identity: La Terraza — Mediterranean sun-baked clay, rustic luxury ── */
    --card-radius: 18px;
    --badge-radius: 8px;
    --tab-radius: 24px;
    --search-radius: 16px;
    --accent-line-h: 3px;
    --card-lift: -4px;

    /* ── Palette ── */
    --bg: #faf3ec;
    --bg-attach: scroll;
    --surface: #ffffff;
    --surface-2: #f4e6d8;
    --fg: #281308;
    --fg-2: #7c4030;
    --fg-3: #c8907a;
    --accent: #b83a18;
    --b1: #e6cfbc;
    --b2: #d2b39c;

    /* ── Header ── */
    --hdr-bg: rgba(250,243,236,0.95);
    --hdr-border: #e6cfbc;
    --hdr-blur: blur(20px);

    /* ── Hero ── */
    --hero-bg: linear-gradient(148deg, #f0ccac 0%, #e8bc98 45%, #faf3ec 100%);
    --hero-border: #e6cfbc;

    /* ── Search input ── */
    --in-bg: #ffffff;
    --in-border: #e6cfbc;
    --in-focus-border: #b83a18;
    --in-focus-shadow: 0 0 0 4px rgba(184,58,24,0.1);
    --in-text: #281308;
    --in-ph: #c8907a;

    /* ── Tabs & filters ── */
    --tab-bg: #ffffff;
    --tab-border: #e6cfbc;
    --tab-text: #c8907a;
    --tab-hbg: #f4e6d8;
    --tab-htext: #7c4030;
    --tab-act-bg: #b83a18;
    --tab-act-border: #b83a18;
    --tab-act-text: #ffffff;
    --tab-act-shadow: 0 4px 18px rgba(184,58,24,0.35);

    /* ── Card front ── */
    --cf-bg: #ffffff;
    --cf-border: #e8d2bc;
    --cf-hborder: #b83a18;
    --cf-hshadow: 0 4px 8px rgba(80,30,10,0.07), 0 16px 40px rgba(80,30,10,0.14);
    --cf-img: #f4e6d8;
    --cf-accent: linear-gradient(90deg, transparent 0%, rgba(184,58,24,0.6) 40%, transparent 100%);
    --cf-accent-op: 1;
    --card-shadow: 0 1px 3px rgba(80,30,10,0.07), 0 4px 14px rgba(80,30,10,0.05);
    --card-hover-shadow: 0 4px 8px rgba(80,30,10,0.07), 0 16px 40px rgba(80,30,10,0.14);

    /* ── Card content ── */
    --cn: #281308;
    --cd: #9a6858;
    --cp: #b83a18;
    --cbg: #f4e6d8;
    --cbc: #e6cfbc;
    --cbt: #9a6858;
    --hint: rgba(0,0,0,0.2);
    --hint-h: rgba(184,58,24,0.6);

    /* ── Card back ── */
    --cb-bg: #faf3ec;
    --cb-border: #e8d2bc;
    --bn: #281308;
    --bp: #b83a18;
    --close-bg: #f4e6d8;
    --close-b: #e6cfbc;
    --close-t: #c8907a;
    --close-hbg: #e8d0bc;
    --close-ht: #281308;
    --bdiv: #f0e2d0;
    --btt: #c8907a;
    --bdesc: #7c4030;
    --fade: #faf3ec;
    --cue-bg: rgba(184,58,24,0.1);
    --cue-text: rgba(184,58,24,0.6);

    /* ── Compact-layout rows ── */
    --row-border: #f0e2d0;
    --row-hover: #fdf7f2;
    --num: #e6cfbc;
    --num-h: #c8907a;

    /* ── Category titles ── */
    --cat-title: #c8907a;
    --cat-sep: #e6cfbc;
    --sep-bg: #e6cfbc;

    /* ── Logo ── */
    --logo-bg: #b83a18;

    /* ── Label colours ── */
    --lg: #4a8a18; --lg-bg: rgba(59,109,17,0.09); --lg-b: rgba(99,153,34,0.16);
    --lo: #b04828; --lo-bg: rgba(193,68,14,0.08); --lo-b: rgba(240,153,123,0.16);

    /* ── Ingredient chips ── */
    --ing-bg: #f4e6d8; --ing-b: #e6cfbc; --ing-t: #7c4030;

    /* ── Nutrition ── */
    --nut-bg: #f4e6d8; --nut-b: #e6cfbc; --nut-v: #281308; --nut-u: #c8907a;

    /* ── Spicy ── */
    --dot-on: #c83010; --dot-off: #e6cfbc; --spicy-t: #c8907a;
}
</style>
