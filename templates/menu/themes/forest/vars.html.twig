<style>
:root {
    /* ── Identity: The Garden Atelier — botanical tasting menu, organic luxury ── */
    --card-radius: 20px;
    --badge-radius: 8px;
    --tab-radius: 24px;
    --search-radius: 16px;
    --accent-line-h: 3px;
    --card-lift: -4px;

    /* ── Palette ── */
    --bg: #f2f6f1;
    --bg-attach: scroll;
    --surface: #ffffff;
    --surface-2: #e4ede2;
    --fg: #152315;
    --fg-2: #385c3c;
    --fg-3: #7aaa80;
    --accent: #1c6030;
    --b1: #c4d8c2;
    --b2: #a8c4a6;

    /* ── Header ── */
    --hdr-bg: rgba(242,246,241,0.95);
    --hdr-border: #c4d8c2;
    --hdr-blur: blur(20px);

    /* ── Hero ── */
    --hero-bg: linear-gradient(150deg, #ccdfca 0%, #d6e8d4 50%, #f2f6f1 100%);
    --hero-border: #c4d8c2;

    /* ── Search input ── */
    --in-bg: #ffffff;
    --in-border: #c4d8c2;
    --in-focus-border: #1c6030;
    --in-focus-shadow: 0 0 0 4px rgba(28,96,48,0.1);
    --in-text: #152315;
    --in-ph: #7aaa80;

    /* ── Tabs & filters ── */
    --tab-bg: #ffffff;
    --tab-border: #c4d8c2;
    --tab-text: #7aaa80;
    --tab-hbg: #e4ede2;
    --tab-htext: #385c3c;
    --tab-act-bg: #1c6030;
    --tab-act-border: #1c6030;
    --tab-act-text: #ffffff;
    --tab-act-shadow: 0 4px 18px rgba(28,96,48,0.32);

    /* ── Card front ── */
    --cf-bg: #ffffff;
    --cf-border: #ccdeca;
    --cf-hborder: #1c6030;
    --cf-hshadow: 0 4px 8px rgba(15,50,20,0.07), 0 16px 40px rgba(15,50,20,0.14);
    --cf-img: #e4ede2;
    --cf-accent: linear-gradient(90deg, transparent 0%, rgba(28,96,48,0.6) 40%, transparent 100%);
    --cf-accent-op: 1;
    --card-shadow: 0 1px 3px rgba(15,50,20,0.07), 0 4px 14px rgba(15,50,20,0.05);
    --card-hover-shadow: 0 4px 8px rgba(15,50,20,0.07), 0 16px 40px rgba(15,50,20,0.14);

    /* ── Card content ── */
    --cn: #152315;
    --cd: #547858;
    --cp: #1c6030;
    --cbg: #e4ede2;
    --cbc: #c4d8c2;
    --cbt: #547858;
    --hint: rgba(0,0,0,0.2);
    --hint-h: rgba(28,96,48,0.6);

    /* ── Card back ── */
    --cb-bg: #f2f6f1;
    --cb-border: #ccdeca;
    --bn: #152315;
    --bp: #1c6030;
    --close-bg: #e4ede2;
    --close-b: #c4d8c2;
    --close-t: #7aaa80;
    --close-hbg: #cce0ca;
    --close-ht: #152315;
    --bdiv: #daeada;
    --btt: #7aaa80;
    --bdesc: #385c3c;
    --fade: #f2f6f1;
    --cue-bg: rgba(28,96,48,0.1);
    --cue-text: rgba(28,96,48,0.6);

    /* ── Compact-layout rows ── */
    --row-border: #daeada;
    --row-hover: #f6faf5;
    --num: #c4d8c2;
    --num-h: #7aaa80;

    /* ── Category titles ── */
    --cat-title: #7aaa80;
    --cat-sep: #c4d8c2;
    --sep-bg: #c4d8c2;

    /* ── Logo ── */
    --logo-bg: #152315;

    /* ── Label colours ── */
    --lg: #3a8a18; --lg-bg: rgba(59,109,17,0.09); --lg-b: rgba(99,153,34,0.16);
    --lo: #c04828; --lo-bg: rgba(193,68,14,0.07); --lo-b: rgba(240,153,123,0.16);

    /* ── Ingredient chips ── */
    --ing-bg: #e4ede2; --ing-b: #c4d8c2; --ing-t: #385c3c;

    /* ── Nutrition ── */
    --nut-bg: #e4ede2; --nut-b: #c4d8c2; --nut-v: #152315; --nut-u: #7aaa80;

    /* ── Spicy ── */
    --dot-on: #D85A30; --dot-off: #c4d8c2; --spicy-t: #7aaa80;
}
</style>
