<style>
:root {
    /* ── Identity: Warm Cream — cream background, red accent, generous
       rounded cards. Unlike the other themes, the accent here is a fixed
       brand red (#C0341D), not restaurant.primaryColor — this theme is a
       deliberately fixed palette, not a per-restaurant customizable one. ── */
    --card-radius: 18px;
    --badge-radius: 8px;
    --tab-radius: 24px;
    --search-radius: 18px;
    --accent-line-h: 3px;
    --card-lift: -4px;

    /* ── Palette ── */
    --bg: #fbf3ea;
    --bg-attach: scroll;
    --surface: #ffffff;
    --surface-2: #f5e9da;
    --fg: #2b2119;
    --fg-2: #7a7268;
    --fg-3: #b3a89a;
    --accent: #c0341d;
    --b1: #ecdfcd;
    --b2: #ddcbb2;

    /* ── Header ── */
    --hdr-bg: rgba(251,243,234,0.95);
    --hdr-border: #ecdfcd;
    --hdr-blur: blur(20px);

    /* ── Hero ── */
    --hero-bg: linear-gradient(148deg, #f3e2ca 0%, #ecd3b2 45%, #fbf3ea 100%);
    --hero-border: #ecdfcd;

    /* ── Search input ── */
    --in-bg: #ffffff;
    --in-border: #e5d6c1;
    --in-focus-border: #c0341d;
    --in-focus-shadow: 0 0 0 4px rgba(192,52,29,0.08);
    --in-text: #2b2119;
    --in-ph: #b3a89a;

    /* ── Tabs & filters ── */
    --tab-bg: #ffffff;
    --tab-border: #e5d6c1;
    --tab-text: #ab9c8a;
    --tab-hbg: #f6ecdc;
    --tab-htext: #7a7268;
    --tab-act-bg: #c0341d;
    --tab-act-border: #c0341d;
    --tab-act-text: #ffffff;
    --tab-act-shadow: 0 4px 16px rgba(192,52,29,0.22);

    /* ── Card front ── */
    --cf-bg: #ffffff;
    --cf-border: #efe4d4;
    --cf-hborder: #c0341d;
    --cf-hshadow: 0 4px 8px rgba(60,30,10,0.06), 0 16px 40px rgba(60,30,10,0.14);
    --cf-img: #f5e9da;
    --cf-accent: linear-gradient(90deg, transparent 0%, #c0341d 40%, transparent 100%);
    --cf-accent-op: 0.5;
    --card-shadow: 0 1px 3px rgba(60,30,10,0.06), 0 4px 14px rgba(60,30,10,0.05);
    --card-hover-shadow: 0 4px 8px rgba(60,30,10,0.06), 0 16px 40px rgba(60,30,10,0.14);

    /* ── Card content ── */
    --cn: #2b2119;
    --cd: #9a8c7a;
    --cp: #c0341d;
    --cbg: #f5e9da;
    --cbc: #e7d8c4;
    --cbt: #9a8c7a;
    --hint: rgba(0,0,0,0.22);
    --hint-h: rgba(0,0,0,0.55);

    /* ── Card back / modal ── */
    --cb-bg: #fdf8f1;
    --cb-border: #efe4d4;
    --bn: #2b2119;
    --bp: #c0341d;
    --close-bg: #ffffff;
    --close-b: #e7d8c4;
    --close-t: #7a7268;
    --close-hbg: #f6ecdc;
    --close-ht: #2b2119;
    --bdiv: #efe4d4;
    --btt: #b3a89a;
    --bdesc: #7a7268;
    --fade: #fdf8f1;
    --cue-bg: rgba(0,0,0,0.07);
    --cue-text: rgba(0,0,0,0.3);

    /* ── Compact-layout rows ── */
    --row-border: #efe4d4;
    --row-hover: #fbf5ec;
    --num: #ddcbb2;
    --num-h: #ab9c8a;

    /* ── Category titles ── */
    --cat-title: #b3a89a;
    --cat-sep: #ecdfcd;
    --sep-bg: #ecdfcd;

    /* ── Logo ── */
    --logo-bg: #c0341d;

    /* ── Label colours ── */
    --lg: #3e8018; --lg-bg: rgba(59,109,17,0.09); --lg-b: rgba(99,153,34,0.16);
    --lo: #b04828; --lo-bg: rgba(193,68,14,0.08); --lo-b: rgba(240,153,123,0.16);

    /* ── Ingredient chips ── */
    --ing-bg: #f5e9da; --ing-b: #e7d8c4; --ing-t: #8a7a63;

    /* ── Nutrition ── */
    --nut-bg: #f5e9da; --nut-b: #e7d8c4; --nut-v: #2b2119; --nut-u: #b3a89a;

    /* ── Spicy ── */
    --dot-on: #c0341d; --dot-off: #e7d8c4; --spicy-t: #b3a89a;

    /* ── Allergen soft box (dish detail modal) ── */
    --allergen-box-bg: rgba(192,52,29,0.06);
    --allergen-box-b: rgba(192,52,29,0.16);
}
</style>
