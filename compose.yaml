services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qr-menu-php
    ports:
      - "8083:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: sh -c "composer install && php -S 0.0.0.0:8000 -t public"
